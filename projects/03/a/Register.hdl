// This file is part of www.nand2tetris.org
// and the book "The Elements of Computing Systems"
// by Nisan and Schocken, MIT Press.
// File name: projects/03/a/Register.hdl

/**
 * 16-bit register:
 * If load[t] == 1 then out[t+1] = in[t]
 * else out does not change
 */

CHIP Register {
    IN in[16], load;
    OUT out[16];

    PARTS:
    // Put your code here:
    Register(in=in[0], load=load, clock=clock, out=out[1]);
    Register(in=in[1], load=load, clock=clock, out=out[2]);
    Register(in=in[2], load=load, clock=clock, out=out[3]);
    Register(in=in[3], load=load, clock=clock, out=out[4]);
    Register(in=in[4], load=load, clock=clock, out=out[5]);
    Register(in=in[5], load=load, clock=clock, out=out[6]);
    Register(in=in[6], load=load, clock=clock, out=out[7]);
    Register(in=in[7], load=load, clock=clock, out=out[8]);
    Register(in=in[8], load=load, clock=clock, out=out[9]);
    Register(in=in[9], load=load, clock=clock, out=out[10]);
    Register(in=in[10], load=load, clock=clock, out=out[11]);
    Register(in=in[11], load=load, clock=clock, out=out[12]);
    Register(in=in[12], load=load, clock=clock, out=out[13]);
    Register(in=in[13], load=load, clock=clock, out=out[14]);
    Register(in=in[14], load=load, clock=clock, out=out[15]);
    Register(in=in[15], load=load, clock=clock, out=out[16]);
}
